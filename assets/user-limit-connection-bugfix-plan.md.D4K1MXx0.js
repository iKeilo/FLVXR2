import{c as e,Q as a,j as s,m as o}from"./chunks/framework.j5cTrcw1.js";const u=JSON.parse('{"title":"用户创建限制项 Bug 排查与改造计划","description":"","frontmatter":{},"headers":[],"relativePath":"user-limit-connection-bugfix-plan.md","filePath":"user-limit-connection-bugfix-plan.md"}'),l={name:"user-limit-connection-bugfix-plan.md"};function n(t,i,d,c,h,p){return a(),s("div",null,[...i[0]||(i[0]=[o(`<h1 id="用户创建限制项-bug-排查与改造计划" tabindex="-1">用户创建限制项 Bug 排查与改造计划 <a class="header-anchor" href="#用户创建限制项-bug-排查与改造计划" aria-label="Permalink to &quot;用户创建限制项 Bug 排查与改造计划&quot;">​</a></h1><h2 id="背景" tabindex="-1">背景 <a class="header-anchor" href="#背景" aria-label="Permalink to &quot;背景&quot;">​</a></h2><p>手机端测试反馈两个问题：</p><ol><li>新增/编辑用户时，<code>流量限制(GB)</code> 和 <code>规则数量</code> 输入框无法清空。删除到最后一位时会被自动恢复为 <code>1</code>，导致只能继续输入成 <code>1x</code>、<code>1xx</code>、<code>1xxx</code>。长按全选后覆盖输入可以绕过。</li><li>创建用户时希望增加“总连接数限制”。当前连接数限制只在规则/转发里配置，用户删减规则后再新增规则时需要重复设置，不方便。</li></ol><p>本次文档只做排查与方案，不进行功能代码修改。</p><h2 id="结论摘要" tabindex="-1">结论摘要 <a class="header-anchor" href="#结论摘要" aria-label="Permalink to &quot;结论摘要&quot;">​</a></h2><ul><li>问题 1 是前端受控数字输入的归一化时机不对，不是后端校验问题。</li><li><code>flow</code> 和 <code>num</code> 当前在输入过程中被立刻夹到 <code>1..99999</code>，移动端删除最后一位产生空字符串时，空字符串会被 <code>Number(&quot;&quot;) || 0</code> 变成 <code>0</code>，再被 <code>Math.max(..., 1)</code> 改回 <code>1</code>。</li><li>问题 2 后端数据模型已有用户级 <code>max_connections</code> 字段，套餐逻辑也会写入 <code>user.max_connections</code>，但用户创建/编辑接口、用户列表返回、前端表单、转发默认继承和核心下发之间没有贯通。</li><li>当前真正下发到 GOST/核心配置的连接数限制来自 <code>forward.max_connections</code>，位置在 <code>go-backend/internal/http/handler/control_plane.go</code>，尚未使用用户级兜底。</li></ul><h2 id="相关代码位置" tabindex="-1">相关代码位置 <a class="header-anchor" href="#相关代码位置" aria-label="Permalink to &quot;相关代码位置&quot;">​</a></h2><h3 id="前端用户表单" tabindex="-1">前端用户表单 <a class="header-anchor" href="#前端用户表单" aria-label="Permalink to &quot;前端用户表单&quot;">​</a></h3><p>文件：<code>vite-frontend/src/pages/user.tsx</code></p><ul><li><code>userForm</code> 状态定义包含 <code>flow</code>、<code>num</code>，但没有 <code>maxConnections</code>。</li><li>新增用户默认值： <ul><li><code>flow: 1000</code></li><li><code>num: 10</code></li></ul></li><li>编辑用户时从 <code>user.flow</code>、<code>user.num</code> 回填。</li><li>提交时直接把 <code>userForm</code> 展开到 <code>createUser</code> / <code>updateUser</code> 请求。</li><li>弹窗里的两个数字输入： <ul><li><code>流量限制(GB)</code>：<code>value={userForm.flow.toString()}</code></li><li><code>规则数量</code>：<code>value={userForm.num.toString()}</code></li></ul></li></ul><p>触发 bug 的核心逻辑：</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">min</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(e.target.value) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  99999</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>当 <code>e.target.value === &quot;&quot;</code> 时：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Number(&quot;&quot;) -&gt; 0</span></span>
<span class="line"><span>0 || 0 -&gt; 0</span></span>
<span class="line"><span>Math.max(0, 1) -&gt; 1</span></span></code></pre></div><p>因此输入框不能处于“临时空值”状态。</p><h3 id="前端-api-类型" tabindex="-1">前端 API 类型 <a class="header-anchor" href="#前端-api-类型" aria-label="Permalink to &quot;前端 API 类型&quot;">​</a></h3><p>文件：<code>vite-frontend/src/api/types.ts</code></p><ul><li><code>UserApiItem</code> 当前包含 <code>flow</code>、<code>num</code>，没有显式 <code>maxConnections</code>。</li><li><code>UserMutationPayload</code> 当前包含 <code>flow</code>、<code>num</code>，没有显式 <code>maxConnections</code>。</li><li><code>ForwardApiItem</code> 已有 <code>maxConnections</code>。</li></ul><h3 id="后端用户创建-更新" tabindex="-1">后端用户创建/更新 <a class="header-anchor" href="#后端用户创建-更新" aria-label="Permalink to &quot;后端用户创建/更新&quot;">​</a></h3><p>文件：<code>go-backend/internal/http/handler/mutations.go</code></p><ul><li><code>userCreate</code> 读取： <ul><li><code>flow := asInt64(req[&quot;flow&quot;], 100)</code></li><li><code>num := asInt(req[&quot;num&quot;], 10)</code></li><li>未读取 <code>maxConnections</code></li></ul></li><li><code>userUpdate</code> 读取： <ul><li><code>flow := asInt64(req[&quot;flow&quot;], 100)</code></li><li><code>num := asInt(req[&quot;num&quot;], 10)</code></li><li>未读取 <code>maxConnections</code></li></ul></li><li>更新用户后会调用： <ul><li><code>h.repo.PropagateUserFlowToTunnels(id, flow, num, expTime, flowResetTime, status)</code></li></ul></li></ul><h3 id="后端用户模型" tabindex="-1">后端用户模型 <a class="header-anchor" href="#后端用户模型" aria-label="Permalink to &quot;后端用户模型&quot;">​</a></h3><p>文件：<code>go-backend/internal/store/model/model.go</code></p><p><code>model.User</code> 已有这些字段：</p><ul><li><code>SpeedLimit</code></li><li><code>MaxRules</code></li><li><code>MaxConnections</code></li><li><code>MaxIPAccess</code></li></ul><p>其中 <code>MaxConnections</code> 对应数据库列：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MaxConnections </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> \`gorm:&quot;column:max_connections;default:0&quot;\`</span></span></code></pre></div><p>但当前人工创建/编辑用户用的是旧的 <code>num</code> 字段控制规则数，并没有暴露 <code>max_connections</code>。</p><h3 id="后端用户仓库方法" tabindex="-1">后端用户仓库方法 <a class="header-anchor" href="#后端用户仓库方法" aria-label="Permalink to &quot;后端用户仓库方法&quot;">​</a></h3><p>文件：<code>go-backend/internal/store/repo/repository_mutations.go</code></p><ul><li><code>CreateUser(...)</code> 参数不包含 <code>maxConnections</code>。</li><li><code>UpdateUserWithPassword(...)</code> / <code>UpdateUserWithoutPassword(...)</code> 参数不包含 <code>maxConnections</code>。</li><li><code>PropagateUserFlowToTunnels(...)</code> 只同步 <code>flow</code>、<code>num</code>、<code>exp_time</code>、<code>flow_reset_time</code>、<code>status</code> 到 <code>user_tunnel</code>。</li></ul><p>套餐相关逻辑已经会写：</p><ul><li><code>user.max_connections</code></li><li><code>user.max_ip_access</code></li><li><code>user.speed_limit</code></li></ul><p>说明数据库层具备承载能力，但用户管理页面未接入。</p><h3 id="规则-转发连接数下发" tabindex="-1">规则/转发连接数下发 <a class="header-anchor" href="#规则-转发连接数下发" aria-label="Permalink to &quot;规则/转发连接数下发&quot;">​</a></h3><p>文件：<code>go-backend/internal/http/handler/control_plane.go</code></p><p>当前核心配置生成只看转发规则自身：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> forward.MaxConnections </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  meta[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;maxConnections&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> forward.MaxConnections</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>所以用户级连接数即使存在，也不会自动影响新建/已有规则。</p><h2 id="bug-1-修复方案-允许输入框临时为空" tabindex="-1">Bug 1 修复方案：允许输入框临时为空 <a class="header-anchor" href="#bug-1-修复方案-允许输入框临时为空" aria-label="Permalink to &quot;Bug 1 修复方案：允许输入框临时为空&quot;">​</a></h2><h3 id="推荐方向" tabindex="-1">推荐方向 <a class="header-anchor" href="#推荐方向" aria-label="Permalink to &quot;推荐方向&quot;">​</a></h3><p>把表单输入态和最终提交态分开。</p><p>数字输入框在用户输入过程中允许空字符串，只有在 <code>blur</code> 或提交时才做最终校验和归一化。</p><h3 id="可选实现" tabindex="-1">可选实现 <a class="header-anchor" href="#可选实现" aria-label="Permalink to &quot;可选实现&quot;">​</a></h3><h4 id="方案-a-最小改动-表单字段改为-number" tabindex="-1">方案 A：最小改动，表单字段改为 <code>number | &quot;&quot;</code> <a class="header-anchor" href="#方案-a-最小改动-表单字段改为-number" aria-label="Permalink to &quot;方案 A：最小改动，表单字段改为 \`number | &quot;&quot;\`&quot;">​</a></h4><p>将 <code>userForm.flow</code>、<code>userForm.num</code> 类型改为：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">flow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: number </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">num</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: number </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>输入时：</p><ul><li>空字符串保留为空。</li><li>非数字忽略或转成空。</li><li>超过上限时可实时裁剪到 <code>99999</code>。</li><li>不在输入时强制最小值 <code>1</code>。</li></ul><p>失焦或提交时：</p><ul><li><code>flow === &quot;&quot;</code> 时恢复默认值或提示必填。</li><li><code>num === &quot;&quot;</code> 时恢复默认值或提示必填。</li><li>最终提交前统一 clamp 到 <code>1..99999</code>。</li></ul><p>优点：改动小，用户体验直观。</p><p>风险：需要检查所有读取 <code>userForm.flow.toString()</code> 的地方，避免空字符串类型引起 TS 报错。</p><h4 id="方案-b-保留数值状态-新增-draft-输入状态" tabindex="-1">方案 B：保留数值状态，新增 draft 输入状态 <a class="header-anchor" href="#方案-b-保留数值状态-新增-draft-输入状态" aria-label="Permalink to &quot;方案 B：保留数值状态，新增 draft 输入状态&quot;">​</a></h4><p>新增：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">userFormDraft</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">setUserFormDraft</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  flow: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1000&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  num: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;10&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>输入框绑定 draft 字符串，提交时再解析到 <code>userForm</code>。</p><p>优点：数字模型保持稳定。</p><p>风险：表单状态重复，新增/编辑/关闭弹窗时要同步 draft，维护成本稍高。</p><h3 id="推荐选择" tabindex="-1">推荐选择 <a class="header-anchor" href="#推荐选择" aria-label="Permalink to &quot;推荐选择&quot;">​</a></h3><p>采用方案 A。当前用户表单只在本组件内使用，<code>number | &quot;&quot;</code> 的影响面可控。</p><h3 id="校验规则" tabindex="-1">校验规则 <a class="header-anchor" href="#校验规则" aria-label="Permalink to &quot;校验规则&quot;">​</a></h3><ul><li><code>flow</code>：最终值范围 <code>1..99999</code>，<code>99999</code> 继续表示不限流量。</li><li><code>num</code>：最终值范围 <code>1..99999</code>。</li><li>空值处理建议： <ul><li>输入过程中允许空。</li><li>点击提交时如果为空，提示“请填写流量限制/规则数量”。</li><li>失焦时可以恢复到上一次合法值，避免用户误以为空值能提交。</li></ul></li></ul><h2 id="bug-2-改造方案-用户级总连接数限制" tabindex="-1">Bug 2 改造方案：用户级总连接数限制 <a class="header-anchor" href="#bug-2-改造方案-用户级总连接数限制" aria-label="Permalink to &quot;Bug 2 改造方案：用户级总连接数限制&quot;">​</a></h2><h3 id="目标语义" tabindex="-1">目标语义 <a class="header-anchor" href="#目标语义" aria-label="Permalink to &quot;目标语义&quot;">​</a></h3><p>新增用户级“总连接数限制”：</p><ul><li><code>0</code> 或留空表示不限制。</li><li>管理员创建/编辑用户时可以设置。</li><li>用户新建规则时，如果规则没有单独设置连接数，则默认继承用户级限制。</li><li>已有规则保持原值，避免后台修改用户后悄悄改变所有规则行为。</li><li>核心配置下发时需要有用户级兜底，保证没有单独规则限制时仍可生效。</li></ul><h3 id="推荐字段" tabindex="-1">推荐字段 <a class="header-anchor" href="#推荐字段" aria-label="Permalink to &quot;推荐字段&quot;">​</a></h3><p>复用现有数据库字段：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>user.max_connections</span></span></code></pre></div><p>不新增数据库列。</p><h3 id="后端改造点" tabindex="-1">后端改造点 <a class="header-anchor" href="#后端改造点" aria-label="Permalink to &quot;后端改造点&quot;">​</a></h3><h4 id="_1-用户列表返回" tabindex="-1">1. 用户列表返回 <a class="header-anchor" href="#_1-用户列表返回" aria-label="Permalink to &quot;1. 用户列表返回&quot;">​</a></h4><p>文件：<code>go-backend/internal/store/repo/repository.go</code></p><p>在 <code>ListUsers()</code> 返回 map 中增加：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;maxConnections&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: u.MaxConnections,</span></span></code></pre></div><h4 id="_2-用户创建" tabindex="-1">2. 用户创建 <a class="header-anchor" href="#_2-用户创建" aria-label="Permalink to &quot;2. 用户创建&quot;">​</a></h4><p>文件：<code>go-backend/internal/http/handler/mutations.go</code></p><p>在 <code>userCreate</code> 读取：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">maxConnections </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> asInt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;maxConnections&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>校验：</p><ul><li>小于 0 则报错或归零。</li><li>建议最大值 <code>99999</code>，避免异常大值进入核心配置。</li></ul><p>传入仓库创建方法。</p><h4 id="_3-用户更新" tabindex="-1">3. 用户更新 <a class="header-anchor" href="#_3-用户更新" aria-label="Permalink to &quot;3. 用户更新&quot;">​</a></h4><p>文件：<code>go-backend/internal/http/handler/mutations.go</code></p><p>在 <code>userUpdate</code> 读取并保存 <code>maxConnections</code>。</p><p>注意：</p><ul><li>管理员手动编辑用户时，应允许将连接数改回 <code>0</code>，表示不限。</li><li>不建议默认把用户级连接数批量覆盖到已有 <code>forward.max_connections</code>，否则会影响已经单独配置过的规则。</li></ul><h4 id="_4-仓库方法" tabindex="-1">4. 仓库方法 <a class="header-anchor" href="#_4-仓库方法" aria-label="Permalink to &quot;4. 仓库方法&quot;">​</a></h4><p>文件：<code>go-backend/internal/store/repo/repository_mutations.go</code></p><p>更新签名：</p><ul><li><code>CreateUser(..., maxConnections int, ...)</code></li><li><code>UpdateUserWithPassword(..., maxConnections int, ...)</code></li><li><code>UpdateUserWithoutPassword(..., maxConnections int, ...)</code></li></ul><p>并在 <code>Create</code> / <code>Updates</code> 中写入：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;max_connections&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: maxConnections</span></span></code></pre></div><p>需要同步更新所有调用点，例如注册逻辑中调用 <code>CreateUser</code> 的位置。</p><h4 id="_5-核心配置下发兜底" tabindex="-1">5. 核心配置下发兜底 <a class="header-anchor" href="#_5-核心配置下发兜底" aria-label="Permalink to &quot;5. 核心配置下发兜底&quot;">​</a></h4><p>文件：<code>go-backend/internal/http/handler/control_plane.go</code></p><p>当前只有：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> forward.MaxConnections </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  meta[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;maxConnections&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> forward.MaxConnections</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>推荐变更为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>effectiveMaxConnections = forward.max_connections</span></span>
<span class="line"><span>if effectiveMaxConnections &lt;= 0:</span></span>
<span class="line"><span>  effectiveMaxConnections = user.max_connections</span></span></code></pre></div><p>需要让生成 control plane 服务配置时能拿到对应用户的 <code>MaxConnections</code>。</p><p>可选做法：</p><ul><li>在查询 <code>ForwardRecord</code> 时 join <code>user.max_connections</code>，增加 <code>UserMaxConnections</code> 字段。</li><li>或在生成配置时构建 <code>userID -&gt; maxConnections</code> 缓存。</li></ul><p>推荐：扩展 <code>ForwardRecord</code>，因为 forward 查询已经持有 <code>user_id</code>，语义清晰。</p><h4 id="_6-新建规则默认值" tabindex="-1">6. 新建规则默认值 <a class="header-anchor" href="#_6-新建规则默认值" aria-label="Permalink to &quot;6. 新建规则默认值&quot;">​</a></h4><p>文件：<code>vite-frontend/src/pages/forward.tsx</code></p><p>新增规则时，如果当前登录/选定用户有 <code>maxConnections &gt; 0</code>：</p><ul><li>表单 <code>maxConnections</code> 默认填用户级限制。</li><li>仍允许规则单独覆盖。</li><li>若留空或填 0，后端下发时仍可由用户级兜底生效。</li></ul><p>这一点对管理员代用户创建规则、用户自己创建规则两种路径都要核对。</p><h3 id="前端改造点" tabindex="-1">前端改造点 <a class="header-anchor" href="#前端改造点" aria-label="Permalink to &quot;前端改造点&quot;">​</a></h3><h4 id="_1-类型补充" tabindex="-1">1. 类型补充 <a class="header-anchor" href="#_1-类型补充" aria-label="Permalink to &quot;1. 类型补充&quot;">​</a></h4><p>文件：<code>vite-frontend/src/api/types.ts</code></p><ul><li><code>UserApiItem.maxConnections?: number</code></li><li><code>UserMutationPayload.maxConnections?: number</code></li></ul><h4 id="_2-用户表单状态补充" tabindex="-1">2. 用户表单状态补充 <a class="header-anchor" href="#_2-用户表单状态补充" aria-label="Permalink to &quot;2. 用户表单状态补充&quot;">​</a></h4><p>文件：<code>vite-frontend/src/pages/user.tsx</code></p><p>新增：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">maxConnections</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: number </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>默认值：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">maxConnections</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span></code></pre></div><p>编辑回填：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">maxConnections</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: user.maxConnections </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span></code></pre></div><h4 id="_3-用户创建-编辑弹窗增加输入项" tabindex="-1">3. 用户创建/编辑弹窗增加输入项 <a class="header-anchor" href="#_3-用户创建-编辑弹窗增加输入项" aria-label="Permalink to &quot;3. 用户创建/编辑弹窗增加输入项&quot;">​</a></h4><p>位置建议放在 <code>规则数量</code> 旁边或其下方。</p><p>文案：</p><ul><li>Label：<code>总连接数限制</code></li><li>Placeholder：<code>不限制</code></li><li>Description：<code>留空或 0 表示不限制；规则未单独设置时使用此限制</code></li></ul><p>输入规则：</p><ul><li>允许清空。</li><li><code>0</code> 表示不限。</li><li>建议范围 <code>0..99999</code>。</li></ul><h4 id="_4-用户列表-卡片展示" tabindex="-1">4. 用户列表/卡片展示 <a class="header-anchor" href="#_4-用户列表-卡片展示" aria-label="Permalink to &quot;4. 用户列表/卡片展示&quot;">​</a></h4><p>可选展示，建议在用户详情或卡片限制区域增加一行：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>连接数：不限 / N</span></span></code></pre></div><p>如果当前界面已经很拥挤，可先只在编辑弹窗展示，不影响功能。</p><h2 id="兼容策略" tabindex="-1">兼容策略 <a class="header-anchor" href="#兼容策略" aria-label="Permalink to &quot;兼容策略&quot;">​</a></h2><ul><li>老数据库：<code>user.max_connections</code> 已在 GORM model 中，AutoMigrate 会补列；无需新增 SQL migration。</li><li>老用户：默认 <code>0</code>，表示不限，不改变现有行为。</li><li>老规则：已有 <code>forward.max_connections</code> 保持优先级；用户级限制只作为兜底。</li><li>套餐用户：套餐已经写入 <code>user.max_connections</code>，新逻辑会让该字段真正参与下发。</li><li>免费/授权逻辑：本次功能属于用户限制能力，暂不绑定商业授权，除非后续明确要求。</li></ul><h2 id="建议优先级" tabindex="-1">建议优先级 <a class="header-anchor" href="#建议优先级" aria-label="Permalink to &quot;建议优先级&quot;">​</a></h2><ol><li>先修复数字输入无法清空，这是确定性前端 bug，影响手机端创建用户。</li><li>再打通用户级 <code>maxConnections</code> 的 CRUD。</li><li>最后处理核心配置兜底与新建规则默认继承。</li></ol><h2 id="测试计划" tabindex="-1">测试计划 <a class="header-anchor" href="#测试计划" aria-label="Permalink to &quot;测试计划&quot;">​</a></h2><h3 id="前端手工测试" tabindex="-1">前端手工测试 <a class="header-anchor" href="#前端手工测试" aria-label="Permalink to &quot;前端手工测试&quot;">​</a></h3><ul><li>手机端新增用户： <ul><li>删除 <code>流量限制(GB)</code> 最后一位后输入框能保持空。</li><li>删除 <code>规则数量</code> 最后一位后输入框能保持空。</li><li>空值提交能看到明确提示，或失焦恢复到合法值。</li><li>输入 <code>123</code> 不会变成 <code>1123</code>。</li><li>长按全选覆盖仍正常。</li></ul></li><li>桌面端新增/编辑用户： <ul><li><code>flow=99999</code> 表示不限仍正常。</li><li><code>num=1</code>、<code>num=99999</code> 正常。</li><li><code>总连接数限制=0/空</code> 表示不限。</li><li><code>总连接数限制=50</code> 能保存并编辑回填。</li></ul></li></ul><h3 id="后端接口测试" tabindex="-1">后端接口测试 <a class="header-anchor" href="#后端接口测试" aria-label="Permalink to &quot;后端接口测试&quot;">​</a></h3><ul><li><code>POST /api/v1/user/create</code><ul><li>带 <code>maxConnections: 50</code> 后数据库 <code>user.max_connections=50</code>。</li><li>带 <code>maxConnections: 0</code> 后表示不限。</li><li>负数应被拒绝或归零，按最终实现保持一致。</li></ul></li><li><code>POST /api/v1/user/update</code><ul><li>可以从 <code>50</code> 改成 <code>0</code>。</li><li>不影响密码留空更新逻辑。</li></ul></li><li><code>POST /api/v1/user/list</code><ul><li>返回 <code>maxConnections</code>。</li></ul></li></ul><h3 id="下发配置测试" tabindex="-1">下发配置测试 <a class="header-anchor" href="#下发配置测试" aria-label="Permalink to &quot;下发配置测试&quot;">​</a></h3><ul><li>创建用户级 <code>maxConnections=50</code>。</li><li>新建规则时不填规则级连接数。</li><li>下发后检查生成服务 metadata 中有：</li></ul><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;maxConnections&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li>如果规则级设置 <code>maxConnections=20</code>，应优先下发 <code>20</code>。</li><li>如果用户级和规则级都是 <code>0</code>，不应写入 <code>maxConnections</code>。</li></ul><h3 id="回归测试" tabindex="-1">回归测试 <a class="header-anchor" href="#回归测试" aria-label="Permalink to &quot;回归测试&quot;">​</a></h3><ul><li>用户删除/新增规则不应丢失用户级连接数。</li><li>套餐购买/后台发放套餐后，<code>pkg.maxConnections</code> 应能成为用户级默认限制。</li><li>规则管理原有 <code>ConnectionLimitField</code> 行为保持：留空代表规则级不限制。</li></ul><h2 id="待确认问题" tabindex="-1">待确认问题 <a class="header-anchor" href="#待确认问题" aria-label="Permalink to &quot;待确认问题&quot;">​</a></h2><ol><li>用户级总连接数限制是“该用户所有规则总和”还是“每条规则默认限制”？ <ul><li>推荐第一阶段实现为“每条规则默认/兜底限制”，因为当前核心配置的 <code>maxConnections</code> 是服务级 metadata。</li><li>如果要做“用户所有规则总连接数总和限制”，需要在 Agent/核心层增加跨服务聚合限制，目前不是简单字段下发能完成。</li></ul></li><li>管理员编辑用户级连接数后，是否要批量同步到已有规则？ <ul><li>推荐默认不批量同步，只作为后续新规则默认值和下发兜底。</li><li>可追加一个“同步到已有规则”的按钮，但不建议默认执行。</li></ul></li><li>空输入失焦时是恢复旧值还是保持空直到提交提示？ <ul><li>推荐保持空，提交时提示；手机端体验最直观。</li></ul></li></ol>`,150)])])}const k=e(l,[["render",n]]);export{u as __pageData,k as default};
